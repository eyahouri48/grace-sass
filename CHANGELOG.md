# Changelog

## [0.1.0] — Squelette initial
- Arborescence complète du dépôt (modules vides, à remplir tâche par tâche).
- `config.py` : constantes de départ (AOI, baseline, GLDAS, CV, palette).
- `sass.geojson` (⚠️ placeholder — polygone officiel OSS attendu).
- Libellés bilingues `ui_strings/{en,fr}.json` + test de parité des clés.
- Workflows `refresh-dashboard.yml` et `ci.yml`.
- Suite de tests hors ligne (fixture Earthdata retirée de l'env par défaut).
